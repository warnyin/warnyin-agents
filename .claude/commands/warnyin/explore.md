---
description: สำรวจ/ตอบคำถามเกี่ยวกับโปรเจกต์แบบ read-only — ไม่สร้าง artifact ใดๆ จบที่คำตอบในแชท
argument-hint: "[คำถาม / สิ่งที่อยากสำรวจ]"
---

ทำหน้าที่เป็น explorer ตาม **playbook กลาง** ของ workflow มาตรฐาน

1. อ่าน `warnyin/workflow/explore.md` ให้ครบก่อน แล้วทำตามทุกหลักการในนั้นอย่างเคร่งครัด
   (**read-only เด็ดขาด — ห้ามสร้าง/แก้/ลบไฟล์ใดๆ**, โค้ดตอบได้ให้ไปอ่านโค้ด, อ้าง `path:line`)
2. Ground ตัวเองด้วย Input ตามข้อ 2 ของ playbook — อ่านเฉพาะส่วนที่เกี่ยวกับคำถาม
3. คำถาม/หัวข้อ: $ARGUMENTS
   - ถ้าไม่ระบุ → ถาม user ว่าอยากสำรวจ/รู้เรื่องอะไร
4. คำถามกว้างที่ต้องกวาดหลายพื้นที่ → fan-out sub-agent แบบ read-only (Explore) ขนาน แล้วสังเคราะห์เป็นคำตอบเดียว
5. ตอบในแชทเท่านั้น — ถ้าพบว่าควรเปิดเป็นงานจริง ให้เสนอ `/warnyin:discovery` หรือ `/warnyin:design` ให้ user ตัดสินใจ
