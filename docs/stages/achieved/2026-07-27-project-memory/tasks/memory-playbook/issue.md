# Issue — memory-playbook (T1)

> Output ของ DESIGN dry-run · playbook: `.warnyin/workflow/stages/design.md` (ข้อ 4.9)
> ผลสแกนหา defer/blocker ของ task นี้ก่อนเข้า BUILD — **สร้างเฉพาะเมื่อพบ issue**
> วิธีสแกน: เดิน implement `memory.md` ทั้ง 9 section ในหัวตาม `spec.md §3` แล้วเทียบกับ contract (`design.md §4`), ไฟล์จริงที่ต้องแก้ (`src/.warnyin/workflow/README.md`), แม่แบบ (`backlog.md`), และ spec ของ task อื่น (T2/T6) ที่กินไฟล์เดียวกัน/assert ไฟล์นี้

## 1. สรุป
- ผลสแกน: blocker **3** ข้อ · defer **7** ข้อ
- สถานะรวม: ☐ แก้ครบ ไม่มี blocker ค้าง / ☑ มี blocker ค้าง (ห้ามเข้า BUILD)

## 2. รายการ issue

| # | ประเภท | จุดที่พบ (ไฟล์/spec/โค้ด) | รายละเอียด | แนวทางแก้ / ข้อสรุป | สถานะ |
|---|---|---|---|---|---|
| 1 | blocker | `design.md:265-270` (C12) · `spec.md:39` · `task.md:51` (A4) | **C12 คำต่อคำใช้ใน `memory.md §2` ไม่ได้เชิงความหมาย** — C12 ถูกเขียนขึ้นสำหรับ **หัวไฟล์ template** จึงใช้คำชี้เฉพาะ "ไฟล์นี้ถูก **commit**" และ "(dead-link gate สแกน**ไฟล์นี้**)". เมื่อ copy คำต่อคำลง §2 ของ playbook "ไฟล์นี้" = `workflow/memory.md` ซึ่ง **ไม่ใช่ไฟล์ memory** และไม่ใช่ไฟล์ที่ dead-link gate ห่วง → กติกากลายเป็นข้อความที่ผิดข้อเท็จจริงในไฟล์ที่เป็น canonical | เลือก 1 ใน 2 (ห้ามเดา — `rule.md:18`): (ก) แก้ C12 ใน `design.md` ให้มี 2 variant (variant template = ของเดิม; variant playbook = "ไฟล์ memory ทั้ง 2 ใบถูก commit …") หรือ (ข) อนุญาตให้ §2 ใส่ **ประโยคนำ 1 บรรทัด** ("ไฟล์ memory ทั้ง 2 ใบอยู่ใต้ข้อบังคับนี้:") แล้วยก C12 เป็น blockquote คำต่อคำ — ระบุมติลง `spec.md §2` ก่อน BUILD | open |
| 2 | blocker | `tasks/release-hygiene/spec.md:36` (M2) ↔ `spec.md:59-63` (§5) | **§5 ของ T1 มีโอกาสสูงที่จะทำให้ gate ของ T6 แดง** — M2 เดิน `.md` ทั้งหมดใต้ `src/.warnyin/workflow/` หา **บรรทัด**ที่มีทั้ง `อัปเดต project memory` **และ** `ไม่มีอะไรเปลี่ยน → ข้าม` แล้ว assert ว่า set ของไฟล์ = 6 ไฟล์เป๊ะ (`stages/*` 5 + `fastlane.md`) และระบุไว้ตรงๆ ว่า "เกิน = … รวมถึงถูก inline ซ้ำใน `memory.md` เอง". แต่ `spec.md §3 §5` สั่งให้เขียน **anchor table จุดเขียน** ที่ "ระบุว่าเป็น conditional (**ไม่มีอะไรเปลี่ยน → ข้าม**)" — การเขียนที่ธรรมชาติที่สุด (แถวตาราง 1 บรรทัด/ประโยคนำของ §5) จะมีทั้ง 2 สตริงในบรรทัดเดียว → `memory.md` เข้าไปเป็นไฟล์ที่ 7 → M2 แดง. **constraint นี้ไม่ปรากฏใน `task.md`/`spec.md`/`rule.md`/`design.md §4` ของ T1 เลย** → builder มองไม่เห็น (wave 1 ไม่เห็นไฟล์ T6) | เพิ่ม constraint ลง `spec.md §3 (§5)` + self-check ใน `task.md §3 ข้อ 3`: **"ห้ามมีบรรทัดใดใน `memory.md` ที่มีทั้ง `อัปเดต project memory` และ `ไม่มีอะไรเปลี่ยน → ข้าม` พร้อมกัน"** — §5 ให้ระบุ conditional แยกบรรทัด/แยกคอลัมน์จากชื่อ hook และ **ห้ามลอก canonical hook wording (C2/C2b/C2c) ลง §5** (เป็นของ T2); §5 เป็น anchor table + "เขียนอะไร" เท่านั้น | open |
| 3 | blocker | `task.md:58` (A11) · `spec.md:122` (T11) ↔ `src/.warnyin/workflow/README.md:21,22,23,71,74,99,105,107` | **A11/T11 พิสูจน์ไม่ได้และขัดกับ A9** — A11 บังคับว่า "**ทั้ง 2 ไฟล์** ไม่มีชื่อรุ่น/ผลิตภัณฑ์ของ harness" แต่ `workflow/README.md` **มีอยู่แล้ว 8 บรรทัด** (`Claude Code`, `Codex`, `Antigravity`) และ A9 บังคับว่า diff ต้องเป็น pure addition ห้ามแก้บรรทัดอื่น → tick A11 ตามตัวอักษรไม่ได้ ไม่ว่าทำอะไร (ตรวจแล้ว: `tasks/release-hygiene/spec.md` M1-M9/P/G **ไม่มี**เคส tool-agnostic → ไม่ทำให้ suite แดง แต่ acceptance เป็นเท็จ) | rescope A11/T11 เป็น **"เนื้อหาที่ task นี้เพิ่มใหม่"** (`memory.md` ทั้งไฟล์ + 3 บรรทัดที่เติมใน `README.md`) ไม่ใช่ทั้งไฟล์ `README.md` | open |
| 4 | defer | `design.md:258-263` (C11 ข/ค) ↔ `README.md:56-57, 76-81` · `standard.md:40` | **C11 คำต่อคำขัดกับรูปแบบ tree เดิม 2 บรรทัด** — (ข) `    scripts/memory-status.mjs   # …` ใช้ indent 4 + prefix `scripts/` แต่ของเดิมเป็น `    scripts/` (dir) แล้วลูกอยู่ indent 6 ชื่อล้วน (`      build-wave.mjs`) → ใส่ตามคำต่อคำจะได้ node `scripts/` ซ้ำ 2 อัน; (ค) `docs/memory.md # …` อยู่ **คอลัมน์ 0** แต่ลูกของบล็อก `docs/` เดิมอยู่ indent 2 (`  troubleshooting.md`) → หลุดชั้นเป็น sibling ของ `docs/`. ชนกับ `standard.md:40` ที่สั่ง "จัดคอลัมน์ให้ตรงแนวเดิมของแต่ละบล็อก". (ก) `    memory.md          # …` ตรงแนวเดิมพอดี ✅ | ยึด **C11 คำต่อคำ** (contract ชนะ — `spec.md:24`; T9/A9 assert substring `scripts/memory-status.mjs` + `docs/memory.md` จึงบีบให้ต้องมี prefix) แล้วรับ cosmetic; หรือ main loop แก้ C11 ใน `design.md` ให้เข้ารูป tree ก่อน BUILD (ทางที่สะอาดกว่า). **ต้องเลือกก่อน build** ไม่งั้น builder จะตัดสินเอง | open |
| 5 | defer | `spec.md:109-123` ↔ `tasks/release-hygiene/spec.md:32-69` | **spec ของ T1 over-claim coverage ของ T6** — `spec.md §7` บอกว่า T1-T12 "เป็นส่วนที่ T6 จะ assert ใน `src/tests/memory.test.mjs`" แต่ M-list จริงของ T6 ครอบแค่ **T1→M1 (heading 9)** และ **T2→M3 (negative-grep)**; **ไม่มี**เคสของ §1 (11 แถว/precedence), C8/C12 ใน §2, schema §3, C13 ใน §4, C9 ใน §8, §9 และ **ไม่มีเคส C11 registry (T9) เลย** → invariant ส่วนใหญ่ของ T1 หลัง integrate เหลือแค่ self-check ตอน build (ไม่ถูกล็อกกัน regress) | main loop ตัดสิน: (ก) เพิ่มเคสให้ T6 (M10 = C11 3 substring, M11 = C8/C9/C12/C13 อยู่ section ที่ถูก) พร้อม bump `MIN_PASS`, หรือ (ข) ลด claim ใน `spec.md §7` ให้ตรงความจริง (เป็น self-check ของ T1) | open |
| 6 | defer | `spec.md:66` (§6) ↔ `design.md:142-157` (C3a/b/c), `design.md:188-198` (C6) | **§6 จะประกาศจุดอ่านที่ไม่มีใครทำ** — spec สั่งให้ §6 ระบุจุดอ่าน "**เริ่ม session** · `discovery.md §2` · `next.md` · `explore.md`" แต่ทั้ง topic ไม่มี hook ที่สั่งอ่านตอน "เริ่ม session": C3 ครอบแค่ 3 ไฟล์, C6 (root doc auto-load) พูดเรื่อง **เขียน** อย่างเดียว, และ `init.md` ที่ Discovery เคยนับเป็นจุดอ่าน (`discovery.md:40`) ถูกตัดออกจาก design แล้ว | เขียน §6 ให้ตรงของจริง — จุดอ่าน = 3 จุดที่มี hook + วงเล็บว่า root doc ทำให้ agent เห็น path ทั้ง 2 ไฟล์ทุก session (ไม่ใช่ "จุดอ่าน" เชิงกลไก); หรือรับเป็น aspiration แล้วเขียนกำกับ | open |
| 7 | defer | `task.md:38` (self-check `lint:md`) · `task.md:57` (A10) · `src/scripts/lint-md.mjs:9,13` | **lint:md ใน worktree ของ wave 1 จะแดงถ้า `memory.md` เขียน markdown-link ชี้ไฟล์ของ task อื่น** — `EXCLUDE_PREFIX` กันเฉพาะ **ไฟล์ที่ถูกสแกน** ไม่ได้กัน **ปลายทางของลิงก์** → ลิงก์ไป `../template/docs/memory.md` (T3) หรือ `scripts/memory-status.mjs` (T5) = dead link ตอน T1 build (เทียบ `tasks/stage-wiring/spec.md:114` ที่ **ห้าม** T2 รัน `lint:md` ใน wave 1 ด้วยเหตุผลเดียวกัน แต่ T1 ถูกสั่งให้รัน) | constraint ให้ builder: อ้าง `template/docs/*`, `scripts/memory-status.mjs`, `docs/memory.md`, `docs/stages/context.md` เป็น **inline-code เท่านั้น** (C13/C12 ทำอยู่แล้ว ✅); markdown-link ใช้ได้เฉพาะไฟล์ที่มีอยู่จริงตอนนี้ (`interop.md`, `minimalism.md`, `triage.md`, `stages/*.md`) | open |
| 8 | defer | `tasks/stage-wiring/spec.md:91` | **doc drift หลังย้าย ownership** — spec ของ T2 ยังเขียนว่า "ตัวไฟล์ template เป็นของ **T1**, script เป็นของ T5" ทั้งที่ template 2 ใบย้ายไป **T3** แล้ว (`design.md:339, 346`) → ถ้า T2 อ่านบรรทัดนี้แล้วเชื่อ อาจสรุป ownership ผิด (แม้ไม่แตะไฟล์จริง) | main loop แก้บรรทัดเดียวใน `tasks/stage-wiring/spec.md:91` ให้เป็น T3 (ไม่กระทบ T1) | open |
| 9 | defer | `src/.warnyin/workflow/README.md:82-83` | **fence เกินมา 1 อัน (pre-existing)** — บรรทัด 82 ปิด fence ของบล็อกโครงสร้าง แล้วบรรทัด 83 เปิด fence ใหม่ค้างไว้ → หัวข้อ "การติดตั้งไปโปรเจกต์อื่น" (85-94) ถูก render อยู่ในโค้ดบล็อก. T1 **แก้ไม่ได้** (A9 pure addition) และไม่กระทบตำแหน่งที่ต้องเติมทั้ง 3 จุด (อยู่ในบล็อก 36-82 ทั้งหมด) | รับทราบ — เสนอเป็น entry `backlog.md`/fastlane แยก ไม่ผูกกับ topic นี้ | open |
| 10 | defer | `task.md:50` (A3) ↔ `design.md:216-219` (C9) · `rule.md:19` | **precedence ถูกบังคับให้เขียน 2 ที่ในไฟล์เดียว** — A3 บังคับ §1 มี precedence และ A7/C9 บังคับ §8 มี precedence คำต่อคำ → ข้อความซ้ำในไฟล์เดียว (ไม่ผิด negative-grep เพราะไฟล์เดียวกัน แต่สวน `minimalism.md`) | เขียน §1 เป็น **สรุป 1 บรรทัด + ชี้ §8** (ฉบับเต็มคำต่อคำอยู่ §8) — pattern เดียวกับที่ spec สั่งไว้แล้วสำหรับ §6 (`spec.md:67`) | open |

## 3. ผลการแก้ไข

<!-- ยังไม่แก้ — dry-run นี้เป็น read-only ตามคำสั่ง; ต้องให้ main loop/user ตัดสิน blocker #1-#3 (และเลือกทางของ #4) แล้วอัปเดต design.md §4 C11/C12 + task.md A11 + spec.md §3(§5)/§7 ก่อนเปิด BUILD ของ T1 -->

### หมายเหตุ: ประเด็นที่ตรวจแล้ว **ไม่เป็น** issue

- **negative-grep `working state (ปัจจุบัน)` ไม่ชนกับ `design.md`** — ขอบเขตสแกนเป็น **`src/` เท่านั้น** ยืนยันตรงกัน 5 จุด: `task.md:49` (A2), `spec.md:113` (T2), `rule.md:9`, `design.md:378-380` (Scenario "กติกาเต็มอยู่ไฟล์เดียว"), `tasks/release-hygiene/spec.md:40` (M3 — "walk `.md` ทั้งหมดใต้ `src/`"). `design.md:83` และไฟล์ task อื่นที่มีสตริงนี้อยู่ใต้ `docs/` จึงอยู่นอกขอบเขต ✅ (ข้อความ `design.md:358` §8 แถวแรกไม่ระบุ root — กำกวมแต่ถูกปิดด้วย §9 Scenario + M3 แล้ว)
- **เนื้อหา `design.md` มีบ้านครบทั้ง 9 section** — §3.4 ตาราง 11 แถว + decision rule 4 ข้อ + precedence → §1 · C8/C12 → §2 · §3.1/§3.2/§3.3 → §3 · C13 + layout + note `.gitignore` → §4 · §5 write-flow → §5 · read-flow → §6 · §5 out-flow (step 1/3/5) → §7 · C9 → §8 · ทบทวน/บีบอัด → §9. ส่วนที่ไม่มีบ้านใน `memory.md` (C2-C7, C10, §6 impact, §9 Spec delta) เป็นของ T2-T6 ตาม ownership ✅
- **ownership ของ T1 disjoint จริง** (`design.md:344`) — `workflow/memory.md` (ใหม่) + `workflow/README.md`; T5 ถูกห้ามแตะ `README.md` และ T2 ถูกห้ามแตะ `memory.md` ระบุไว้แล้วที่ `design.md:351` ✅
- **pointer ของ §7 ตรงกับ `ship.md` จริง** — §3 ข้อ 7 (`เก็บ learned-rule ให้หมด`) มีอยู่, §4 step 1 = รวบ candidate, step 3 = promotion plan, step 5 = execute (ข้อย่อยปัจจุบัน 1-7 → C4b เพิ่มข้อ 8) ✅
- **A3 นับ 11 แถวได้จริง** — `design.md §3.4` มีแถวข้อมูล 11 แถวพอดี; ข้อควรระวังตอนเขียน: §1 ต้องมี **ตารางเดียว** (decision rule เขียนเป็น numbered list) ไม่งั้น T3 (`spec.md:114`) นับเกิน
