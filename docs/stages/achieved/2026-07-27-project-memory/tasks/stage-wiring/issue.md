# Issue — stage-wiring

> Output ของ DESIGN dry-run · playbook: `.warnyin/workflow/stages/design.md` (ข้อ 4.9)
> ผลสแกนหา defer/blocker ของ task นี้ก่อนเข้า BUILD — **สร้างเฉพาะเมื่อพบ issue**

## 1. สรุป
- ผลสแกน: blocker **1** ข้อ · defer **8** ข้อ
- สถานะรวม: ☐ แก้ครบ ไม่มี blocker ค้าง / ☑ มี blocker ค้าง (ห้ามเข้า BUILD)

> **anchor ทั้ง 13/14 จุดตรวจแล้วมีจริงและไม่กำกวมทุกจุด** — บรรทัดเดิมที่ spec ยกมา (C3a/C3b/C3c) ตรงกับไฟล์จริงคำต่อคำ · ท้าย §4 ของทั้ง 5 stage ระบุตำแหน่งได้ชัด · `ship.md §4 step 5` มีข้อย่อยถึง **7** จริง (C4b เป็นข้อ 8 ได้) · `ship.md §4 step 1` มี bullet `planned:` / `emergent:` / `entry แต่ละตัว:` จริง (C4a แทรกกลางได้) · `fastlane.md §3` เป็นตาราง **3 คอลัมน์** ตรงกับ C2c · `ship.md §6` นับ `- [ ]` ได้ **11** พอดี (→12) · `explore.md` ข้อ 4 มี sub-bullet `achieved/` ห้อยอยู่จริง (คงไว้ได้) · path variant `../` vs `./` ไม่ขัดกับที่อยู่ไฟล์ · ไม่มีสตริง hook เดิมหลงเหลือ (idempotent สะอาด) · ไม่ชนไฟล์ของ task อื่นตาม `design.md §7`

## 2. รายการ issue

| # | ประเภท | จุดที่พบ (ไฟล์/spec/โค้ด) | รายละเอียด | แนวทางแก้ / ข้อสรุป | สถานะ |
|---|---|---|---|---|---|
| 1 | **blocker** | `task.md §5` (bullet 5) · `spec.md §7` S1 · `design.md §9` Scenario "candidate ถูกรวบก่อนขั้นอนุมัติ" — เทียบกับ `src/.warnyin/workflow/stages/ship.md:35` และ `:51` | **acceptance พิสูจน์ไม่ได้ (จะแดงแน่นอน)** — เกณฑ์เขียนว่า "index ของ `**project memory:**` **น้อยกว่า** index ของ `promotion plan`" แต่ไฟล์จริงมีคำว่า `promotion plan` **2 จุด**: `ship.md:35` (§3 ข้อ 2 "สรุป promotion plan ...") และ `ship.md:51` (§4 step 3). C4a ถูกแทรกที่ §4 step 1 ≈ บรรทัด 49 → `indexOf('**project memory:**') ≈ 49 > indexOf('promotion plan') = 35` → **fail**. T2 แก้ไม่ได้เพราะ `ship.md §3` เป็นข้อความเดิมที่ `rule.md §1.7` + `spec.md §2.3` ห้ามแตะ | แก้ที่ DESIGN: เปลี่ยน ordering proxy ให้ชี้ **occurrence ของ step อนุมัติ** โดยเฉพาะ เช่นใช้ needle `**สรุป promotion plan + ขออนุมัติ (ครั้งเดียว):**` (unique ที่ §4 step 3) หรือใช้ `lastIndexOf('promotion plan')` แล้วอัปเดตให้ตรงกันทั้ง 3 ที่ (`task.md §5`, `spec.md §7` S1, `design.md §9`) — **ห้ามแก้ด้วยการย้าย/แก้ข้อความ `ship.md §3`** | open |
| 2 | defer | `task.md §4` (บรรทัดสรุป) · `spec.md §2` (หัวข้อ) | **นับจุดแก้ไม่ตรง** — ระบุ "13 จุด · 8 ไฟล์" แต่รายการจริงรวมได้ **14**: C2×4 + C2b + C2c + C3a/b/c + C4a/b/c + C5a/b; ตารางต่อไฟล์ก็ได้ 2+1+1+1+4+3+1+1 = 14 | แก้ตัวเลขเป็น 14 ทั้ง `task.md §4` และ `spec.md §2` (ตารางต่อไฟล์ถูกอยู่แล้ว ไม่ต้องแก้) — ไม่ block เพราะตารางเป็น source ที่ชัดกว่า | open |
| 3 | defer | `src/.warnyin/workflow/next.md:41` + C5b (`design.md §4`) | **การต่อท้ายทำให้ตัวคั่นชิดกัน** — บรรทัดเดิมจบด้วย `... → ระบุ "–")` และ C5b ขึ้นต้นด้วย `· ` (ไม่มีช่องว่างนำ) → ต่อตรงๆ ตาม `standard.md §2.3` ("ต่อได้ตรงๆ") จะได้ `"–")· memory:` ซึ่งขัดสไตล์เดิมของบรรทัดเดียวกันที่ใช้ ` · ` (เว้นวรรคสองข้าง) | ตัดสินให้ **เติมช่องว่าง 1 ตัวก่อน `·`** — string-equality assert ของ T6 ยังผ่านเพราะ C5b ถูก match แบบ substring; ระบุข้อนี้ใน `standard.md §2.3` ให้ชัด (C5a ที่ขึ้นต้น `; ` ไม่มีปัญหา — ต่อตรงๆ ถูกแล้ว) | open |
| 4 | defer | C5b (`design.md §4`) อ้าง "เกณฑ์ใน `memory.md` §3" เทียบกับ C1 heading freeze | **pointer ข้าม task อาจชี้ผิด section** — C1 freeze ให้ `memory.md §3 = ## 3. Schema` แต่เกณฑ์เชิงตัวเลขอยู่ที่ `design.md §3.3` (เกณฑ์ "ไม่บวม") ซึ่งยังไม่ระบุว่าจะลงที่ section ไหนของ `memory.md` (§3 Schema / §4 layout+lifecycle / §9 ทบทวน) | T2 copy ตาม contract ตามเดิม (ไม่คิดเอง) — แต่ต้อง track ให้ **T1** วางเกณฑ์ §3.3 ไว้ใต้ `memory.md ## 3.` และให้ **T6** assert ความสอดคล้อง; ถ้า T1 วางที่อื่น → ต้องแก้ C5b ที่ `design.md §4` ไม่ใช่แก้ `next.md` | open |
| 5 | defer | `task.md §3` ข้อ 3 · `spec.md §2.1` แถว 6 เทียบ `src/.warnyin/workflow/fastlane.md:37-45` | **ชื่อ anchor คลาดเคลื่อน + คอลัมน์ 3 เบี่ยงจาก convention** — ตารางที่ §3 คือ "ลำดับขั้นการทำงาน" (5 แถว · header `# \| step \| เจ้าของกฎ (ไปอ่านที่นี่)`) ไม่ใช่ "ตาราง skip-list" (canonical อยู่ `triage.md#fast-track-skip-list`); และ `fastlane.md:37` กำกับว่า "pointer-per-row — เนื้อกฎอยู่ที่เจ้าของกฎ (คอลัมน์ขวา)" แต่คอลัมน์ 3 ของ C2c ใส่ **สรุปกฎ + ลิงก์** ไม่ใช่ pointer ล้วน | anchor ยังชัด (แถวต่อจากแถว 5 `ship-lite`) → **ไม่ block**; แก้ถ้อยคำใน `task.md §3`/`spec.md §2.1` เป็น "ตาราง §3 ลำดับขั้นการทำงาน" · ยืนยันว่า acceptance ("สรุป 1 บรรทัด + ลิงก์กลับ `memory.md`") ยอมรับรูปแบบของ C2c แล้ว จึงคง C2c ไว้คำต่อคำ | open |
| 6 | defer | `rule.md §1.9` (LF ล้วน) · `standard.md §1` เทียบ `git ls-files --eol` ของทั้ง 8 ไฟล์ | **working tree ของทั้ง 8 ไฟล์เป็น CRLF อยู่แล้ว** (`i/lf w/crlf attr/text=auto eol=lf`) — index เป็น LF และ `.gitattributes` (`* text=auto eol=lf`) normalize ตอน commit อยู่แล้ว; `src/tests/eol.test.mjs` คุมเฉพาะ `.mjs` ไม่คุม `.md` → กฎ "LF ล้วน" เป็นจริงที่ระดับ index โดยอัตโนมัติ | ระบุใน `standard.md §1` ให้ชัดว่า build agent **ห้ามแปลง EOL ทั้งไฟล์** (จะได้ diff ทั้งไฟล์โดยไม่จำเป็น + เสี่ยงชนตอน `git checkout <branch> -- <files>` ของ BUILD integrate) — ให้ append/replace โดยคง EOL เดิมของไฟล์ | open |
| 7 | defer | `spec.md §7` R2 · `design.md §9` Scenario "ไม่มีคำสั่งอ่านซ้ำในไฟล์เดียว" | **เกณฑ์ semantic → deterministic เฉพาะเมื่อนับด้วย full path** — ถ้า T6 นับบรรทัดที่มี `docs/stages/context.md` จะได้ 1 ต่อไฟล์ ✔ แต่ถ้านับ bare `context.md` จะได้ `next.md` = **3** บรรทัด (`next.md:18` "ยกเว้น `achieved/` และ `context.md`" · `:20` · `:47` "ห้ามสร้าง/แก้/ลบไฟล์ รวมถึง `context.md`") → แดงทั้งที่ไม่ผิด | ระบุใน `spec.md §7` R2 ให้ชัดว่า needle = **`docs/stages/context.md` (full path)** เพื่อให้ T6 assert ได้แบบ deterministic | open |
| 8 | defer | `standard.md §2.1` เทียบ `src/.warnyin/workflow/stages/design.md:123-124` | **`design.md` เป็นไฟล์เดียวที่ท้าย §4 เป็น blockquote 2 บรรทัด** (ไม่ใช่ step list) — ถ้าวาง C2 ต่อโดยไม่คั่นบรรทัดว่าง CommonMark จะรวมเป็น blockquote เดียวกับ 2 บรรทัดเดิม (เปลี่ยนการนำเสนอ) | ยืนยันว่าต้อง **คั่นบรรทัดว่าง 1 บรรทัดก่อน `> **★ อัปเดต project memory`** เสมอ (ตาม `standard.md §2.1` อยู่แล้ว) และเพิ่มตัวอย่างเคส blockquote-ต่อ-blockquote ลง `standard.md §2.1` | open |
| 9 | defer | `design.md §8` แถว structural (T6) — "write-intent string ปรากฏครบ 5 stage + fastlane (นับให้เท่ากับจำนวนที่คาด)" | **exact-count เสี่ยง off-by-one ข้าม task** — `memory.md` ของ T1 (§5 "Write points (hook ต่อ stage)") มีโอกาสมีวลี `อัปเดต project memory` ด้วย → นับทั้ง `src/.warnyin/workflow/` จะได้ 7 ไฟล์ ไม่ใช่ 6 | ระบุ scope การนับให้ T6 ชัด: นับเฉพาะ `stages/{discovery,design,build,verify,ship}.md` + `fastlane.md` (whitelist) ไม่ใช่ walk ทั้งโฟลเดอร์ | open |

> - **blocker** — ทำให้ implement ตาม spec ไม่ได้ (ขัดแย้งกับโค้ดจริง/task อื่น, ข้อมูลขาด, dependency ผิด) → ต้องแก้ DESIGN ก่อนเข้า BUILD
> - **defer** — ตัดสินใจ/ทำทีหลังได้ ไม่ block การเริ่ม BUILD แต่ต้องบันทึกและให้ user รับทราบ

## 3. ผลการแก้ไข

<!-- ยังไม่ได้แก้ — dry-run read-only รอบนี้รายงานอย่างเดียว -->

**หมายเหตุที่ตรวจแล้วว่า "ไม่ใช่ issue" (บันทึกกันตรวจซ้ำ):**

- **dead link `](../memory.md)` / `](./memory.md)` ระหว่าง wave 1** — ยืนยันกับ `src/scripts/lint-md.mjs` แล้ว (`SCAN_ROOTS = ['src','docs']`, `EXCLUDE_PREFIX` มีแค่ `src/.warnyin/template/` + `docs/stages/achieved/`; ลิงก์อยู่นอก code fence จึงถูก `LINK_RE` จับ) → จะแดงจนกว่า T1 สร้าง `src/.warnyin/workflow/memory.md` — **คาดหมายแล้ว** ตาม `spec.md §7` + `rule.md §1.6`; BUILD full-gate (`build.md §4` ข้อ 6) รันหลัง wave 2 จึงเขียวทัน
- **เทสเดิมของ `fastlane.md`** — `src/tests/fastlane.test.mjs` B1/B2/B3/E1/F1/F2 ไม่ถูกกระทบจากการเพิ่มแถว C2c (ไม่มี assert จำนวนแถว · C2c ไม่มี token hard-floor/`config-protection`/`investigate-before-edit` · แถวใหม่อยู่หลังแถว 2 "แก้โค้ด" จึงไม่พลิกลำดับของ E1)
- **การซ้อนของ numbered list ที่ `ship.md §4 step 5`** — blockquote ที่ `ship.md:57` (indent 3) ตัดลิสต์ย่อยเป็น 2 ก้อน (1-3 / 4-7) แต่ก้อนหลังเริ่มที่ `4.` และเรนเดอร์ต่อเนื่อง → เติม `   8.` ต่อจาก `   7.` ได้ตามปกติ (indent 3 space ตรงกับ C4b)
