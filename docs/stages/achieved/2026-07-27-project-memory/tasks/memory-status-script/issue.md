# Issue — memory-status-script

> Output ของ DESIGN dry-run · playbook: `.warnyin/workflow/stages/design.md` (ข้อ 4.9)
> ผลสแกนหา defer/blocker ของ task นี้ก่อนเข้า BUILD — **สร้างเฉพาะเมื่อพบ issue**

## 1. สรุป
- ผลสแกน: blocker **0** ข้อ · defer **7** ข้อ
- สถานะรวม: ☑ แก้ครบ ไม่มี blocker ค้าง / ☐ มี blocker ค้าง (ห้ามเข้า BUILD)
- **เดิน implement ในหัวจนจบแล้ว** — parse contract `spec.md §4/§4.1` ใช้จริงได้ทุกเคสของ schema 6 คอลัมน์ (`design.md §3.1`); ไม่มีข้อไหนที่ทำให้เขียนโค้ดไม่ได้ ทุกข้อด้านล่างแก้ได้ระหว่าง BUILD ด้วยการตัดสินใจตอนเขียน (D2–D7) หรือเป็นงานของ task อื่น (D1)

## 2. รายการ issue

| # | ประเภท | จุดที่พบ (ไฟล์/spec/โค้ด) | รายละเอียด | แนวทางแก้ / ข้อสรุป | สถานะ |
|---|---|---|---|---|---|
| 1 | defer | `design.md §3.1` แถวตัวอย่าง + `spec.md §4` parse contract (cross-task: T3 เจ้าของ `template/docs/memory.md`) | parse contract บอกว่า "แถวตัวอย่างที่คอลัมน์แรกไม่ใช่ตัวเลข = ไม่นับ" แต่ **แถวตัวอย่างใน `design.md §3.1` เองขึ้นต้นด้วย `\| 1 \|`** และสถานะเซลล์สุดท้ายเป็น `` `open` `` → ถ้า T3 copy แถวนี้ลง template ตรง ๆ **โปรเจกต์ที่เพิ่งติดตั้งจะรายงาน `open 1` ทั้งที่ยังไม่มี entry จริง**; เพิ่มเติม parser ไม่มี fenced-code awareness (ต่างจาก `lint-md.mjs` ที่มี `CODE_RE`) → ตารางตัวอย่างที่อยู่ใน code fence ก็ถูกนับ | แจ้ง T3: แถวตัวอย่างใน template ต้องให้คอลัมน์แรก **ไม่ใช่ตัวเลข** (เช่น `\| ตัวอย่าง \|`) หรือไม่ใส่แถวตัวอย่างเลย; แจ้ง T6 เพิ่ม structural assert ว่า template ไม่มีแถวที่คอลัมน์แรกเป็นตัวเลข — **T5 ไม่ต้องแก้อะไร** (fixture ของ T5 เป็น string ในไฟล์เทสเอง) | open |
| 2 | defer | `spec.md §2` ("pure — ไม่แตะ fs/เวลา/argv") ขัดกับ `spec.md §2.1` (`now` default `new Date()`) | ไม่ขัดกับ **signature** ของ C10 (ยังเป็น object ตัวเดียว เพิ่ม property optional) แต่ขัดกับคำว่า **pure**: ถ้า default เป็น `new Date()` แล้ว `main()` เรียกแบบ `summarize({contextText, memoryText})` ตาม `§3` data-flow → ฟังก์ชันอ่านนาฬิกาเอง; และเปิดช่อง **time-bomb**: fixture ที่ hardcode วันที่ (เช่น `2026-07-27`) ถ้าเคสไหน assert `flags` โดยไม่ส่ง `now` จะเริ่มแดงเองเมื่อเวลาจริงผ่าน 90 วัน | ตัดสินตอน BUILD: ให้ `main()` ส่ง `now: new Date()` **อย่างชัดเจน** และให้ `summarize()` ถือว่า `now` ที่ขาด = ข้ามการคำนวณ `stale-open` (ยังคง pure 100%) — เข้ากันได้กับ U1–U12 ทุกเคสตามที่ spec เขียนไว้; **ทุกเคสที่มี fixture วันที่ ต้องส่ง `now` เสมอ** และสร้างด้วย UTC (`Date.UTC(...)` / `...T00:00:00Z`) ตาม `§5` | open |
| 3 | defer | `spec.md §7 N1` + `spec.md §8` vs `standard.md §2` (สั่งให้มี "header comment อธิบายหน้าที่+ข้อจำกัด security") | N1 อ่านซอร์สตัวเองแล้ว `.includes()` ทีละ token — ถ้า BUILD copy ข้อความ `§8` ลง header comment ตามที่ `standard.md` สั่ง (`ห้าม import node:child_process ...`) เทสจะ **แดงทันทีจากคอมเมนต์ตัวเอง** (false-positive); เช่นเดียวกับ token เขียนไฟล์ (`writeFileSync` ฯลฯ) ถ้าเขียนในคอมเมนต์ | 2 ชั้น: (ก) เขียน header comment โดย **ไม่พิมพ์ token เต็ม** — ใช้ precedent จริงของ `validate-topic.mjs` ที่เขียนว่า `ไม่มี child_process/network/write` (ไม่มี prefix `node:`); (ข) ให้ N1 ตรวจ **เฉพาะบรรทัด import** ด้วย regex line-scoped เช่น `/^\s*import[^\n]*['"]node:child_process['"]/m` แทน `.includes()` ทั้งไฟล์ แล้วคง `.includes()` ไว้เฉพาะ token API เขียนไฟล์ · หมายเหตุ `node:http` เป็น substring ของ `node:https` → ข้อความ error ต้องไม่ทำให้เข้าใจผิด | open |
| 4 | defer | `spec.md §7 U11` (+ U10) | U11 เขียนว่า "context 60 บรรทัด + `open` 30 แถว + entry `open` อายุ 90 วันพอดี → `flags` เป็น `[]`" — ถ้าอ่านว่า entry อายุ 90 วัน **เป็นแถวที่ 31** จะได้ `counts.open = 31 > 30` → `open-over-limit` ยิง → **เทสแดงโดยไม่ตั้งใจ ทั้งที่โค้ดถูก**; และถ้า fixture context 60 บรรทัดลืมใส่บรรทัดขึ้นต้น `#` จะโดน C13 ตัดเหลือ `contextLines = 0` → เคส boundary **ผ่านแบบ vacuous** | ระบุให้ชัดตอนเขียน: entry อายุ 90 วัน **เป็นหนึ่งใน 30 แถวนั้น** (ไม่ใช่แถวที่ 31); และ U10/U11 ต้อง assert **ค่าจริง** เพิ่ม (`contextLines === 61/60`, `counts.open === 31/30`) ไม่ใช่ assert แค่ `flags` — กัน boundary test ที่ผ่านเพราะ input ไม่ถึงเกณฑ์ (`docs/rule.md §5` anti-false-green) | open |
| 5 | defer | `spec.md §7 S3` vs `spec.md §6` ("`unknown > 0` → พิมพ์จำนวน · ห้ามพิมพ์เลขแถว/เนื้อเซลล์") | negative property "ห้ามพิมพ์เนื้อ entry" **falsifiable แต่หลวม**: (ก) S3 assert แค่ว่า 2 สตริงไม่อยู่ใน stdout → **vacuous ถ้า script crash แล้ว stdout ว่าง** (spec ไม่ได้สั่ง assert `status === 0` และ stdout ไม่ว่างในเคสนี้) · (ข) ครอบแค่ 2 คอลัมน์ — ปล่อยให้ leak คอลัมน์ `ประเภท`/เลขแถวได้ · (ค) ไม่มีเคสไหนคุมบรรทัด ⚠ ของ `unknown` ซึ่งเป็นจุดที่ spec เตือนตรง ๆ ว่าห้าม echo เนื้อเซลล์ · (ง) เช็คแค่ stdout ไม่เช็ค stderr | ทำให้ falsifiable แน่น: ใส่ **sentinel เฉพาะตัวทุกเซลล์** ของทุกแถวใน fixture (บทเรียน/evidence/ประเภท/สถานะนอก closed-set) แล้ว assert ว่า `stdout + stderr` ไม่มี sentinel ตัวใดเลย **พร้อมกับ** assert `status === 0` และ stdout มีบรรทัดหัวรายงาน (พิสูจน์ว่ารันจริง); เพิ่มแถวสถานะนอก closed-set ลง fixture ของ S3 เพื่อบังคับให้บรรทัด ⚠ ถูก render แล้วยังไม่ leak | open |
| 6 | defer | `spec.md §4.1` ("ตัด element สุดท้ายทิ้งเมื่อ trim แล้วว่าง") vs `spec.md §7 U5` (fixture "เซลล์ว่าง") | ตรวจแล้ว index ถูกต้องกับ schema 6 คอลัมน์: `split('\|')` ของแถวปิดหัว-ท้ายด้วย `\|` ให้ 8 element → ตัดหัว 1 + ท้าย 1 = 6 เซลล์ → `cells[0]` = `#`, `cells[5]` = สถานะ, `cells[4]` = วันที่ ✓ และแถวที่ไม่ปิดท้ายด้วย `\|` ก็ยังถูก ✓ **แต่** ถ้าแถวมีเซลล์สถานะ **ว่าง** และปิดท้ายด้วย `\|` จะเกิด trailing empty **สองตัว** → ถ้า implement เป็น while-loop/`filter` จะกินเซลล์สถานะไปด้วย ทำให้ `cells[length-2]` เลื่อนจาก "วันที่" ไปเป็น "ประเภท" | ยึด **ตัดครั้งเดียว** ตามตัวอักษรของ `§4.1` (ห้าม loop/`filter`) — ผล `counts.unknown` เท่ากันทั้งสองแบบ แต่ index ของคอลัมน์วันที่ต่างกัน; เขียนเคส U5 ให้มีทั้งแบบปิดท้าย `\|` และไม่ปิด · ยืนยันแล้วว่า `cells[length-2]` ปลอดภัยกับแถวสั้น (`cells.length === 2` → ชี้กลับไปที่คอลัมน์ `#` ซึ่งเป็น `/^\d+$/` จึงไม่มีวัน match `/^\d{4}-\d{2}-\d{2}$/` → ข้าม staleness เงียบ ๆ ตามสเปก ไม่ throw) | open |
| 7 | defer | `spec.md §6` + `§4.2` (`lastUpdated`) | `lastUpdated` เป็น **ข้อความอิสระ** จาก `context.md` ที่ถูกพิมพ์ออก stdout ทั้งบรรทัด — เป็นสตริงเดียวในรายงานที่ไม่มีขอบเขต; `§4.2` คืน "บรรทัดถัดไปที่ไม่ว่าง" ซึ่งอาจเป็น prose/`>` note/`---` ก็ได้ ขัดกับเจตนา "พิมพ์เฉพาะตัวเลข/วันที่/flag" (แม้จะไม่ใช่ "เนื้อ entry" ตามนิยาม จึงไม่ผิด acceptance) | ตัดสินตอน BUILD: cap ความยาวที่พิมพ์ (เช่น ตัดที่ ~60 ตัวอักษร) หรือพิมพ์เฉพาะเมื่อขึ้นต้นด้วย `YYYY-MM-DD` มิฉะนั้นพิมพ์ `–` — **ห้ามเพิ่ม flag/knob ใหม่** (`rule.md` minimalism); ถ้าเลือกไม่ทำ ให้ note ไว้เฉย ๆ ว่าเป็น accepted surface | open |

## 3. ผลการแก้ไข

**ยังไม่แก้ `design.md`** — ไม่มี blocker; D2–D7 เป็นการตัดสินใจระดับ implementation ที่ทำได้ใน BUILD โดยไม่ขัด C10 และ D1 เป็นงานของ T3/T6

### สิ่งที่ตรวจแล้วว่า **ไม่มีปัญหา** (บันทึกไว้กัน dry-run ซ้ำ)
- **เคส legend-only เพียงพอ** — legend ตาม `design.md §9` เขียนเป็น prose `ระบุประเภท ∈ {...} และสถานะ ∈ {...}` **ไม่ขึ้นต้นด้วย `\|`** → ถูกกรองตั้งแต่เงื่อนไขแรก; แม้เขียน legend เป็นแถวตาราง (`\| ประเภท \| ... \|`) ก็ยังตกเพราะคอลัมน์แรกไม่ใช่ตัวเลข; separator `\|---\|` → `---` ไม่ใช่ตัวเลข ✓ **ทางหลุดเดียวคือแถวที่คอลัมน์แรกเป็นตัวเลข ASCII** = D1 · หมายเหตุ: เลขไทย (`๑`) ไม่ถูกนับ (ตั้งใจ — `/^\d+$/` ASCII ล้วนตาม `§4.1`)
- **staleness deterministic ได้** — `Math.floor((now - Date.parse(...)) / 86400000)` ตาม `§5` ให้ 90 วันพอดี = ไม่ stale, 91 = stale ตราบใดที่ `now` เป็น UTC midnight; วันที่อนาคต → อายุติดลบ → ไม่ stale อัตโนมัติ ✓
- **ไม่มี test ปัจจุบันที่พังเพราะมีไฟล์ใหม่ใต้ `workflow/scripts/`** — `fastlane.test.mjs` เช็ค registry ของ `fastlane.md` เท่านั้น (ไม่ enumerate ทั้งโฟลเดอร์), `installer.test.mjs` ไม่ assert จำนวนไฟล์, `eol.test.mjs` ครอบไฟล์ใหม่อัตโนมัติ (ต้องเป็น LF) → **spec ข้อ "ห้ามพึ่ง `next.md`/`README.md`" ครบถ้วนแล้ว** ทั้งใน `task.md §2 ★`, `rule.md §1 ★` และ `spec.md §7` (ไม่มีเคสไหนแตะ 2 ไฟล์นั้น)
- **packaging ไม่ต้องแก้** — `package.json` `files` มี `src/.warnyin` และ `verify-pack` `ALLOWED_PREFIX` มี `src/.warnyin/` → ไฟล์ใหม่ติด tarball เอง (แต่ยัง**ไม่มี assert ยืนยัน** — เป็น gate ลวงชนิดเดียวกับที่ `design.md §6` แถว `packaging` ระบุไว้ให้ T6 ปิด)
- **`MIN_PASS` / `pass !== tests`** — `MIN_PASS = 46` เป็น **floor** เพิ่มเคสไม่มีทางทำให้ตก; `pass !== tests` ตกได้เฉพาะเมื่อมี `t.skip()` (ห้ามอยู่แล้ว) · ⚠ ข้อควรระวัง 2 ข้อ: (1) `npm test` ในเครื่อง = `node --test` **เปล่า ๆ ไม่ผ่าน gate** — gate อยู่ใน CI (`npm test 2>&1 \| node src/scripts/check-test-count.mjs`) ต้อง pipe เองเพื่อยืนยัน `pass === tests` · (2) `readMetric()` ใช้ match **ตัวแรก** ของทั้ง output → **ห้าม `console.log` stdout ที่ capture มาจาก child** ในเทส (ถ้าบังเอิญมีบรรทัดรูป `pass <n>` จะทำให้ gate อ่านตัวเลขผิด); รูปแบบรายงานตาม `§6` ไม่มีบรรทัดแบบนั้น
