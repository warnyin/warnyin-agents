# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code local-only settings (ไม่ commit ค่าเฉพาะเครื่อง)
.claude/settings.local.json

# Dependencies / build (เผื่อ project จริงมาใช้ template นี้)
node_modules/
dist/
build/
*.log
*.tgz

# skill ที่ติดตั้ง local — ไม่ใช่ส่วนหนึ่งของ workflow ที่แจกจ่าย
.agents/
.claude/skills/
skills-lock.json

# dogfood layer ที่ติดตั้งจาก release (regen ด้วย `npm run setup:dogfood`)
# ★ ทุกบรรทัด root-anchored (`/` นำหน้า) — กันไป match source ใน src/.claude, src/.warnyin
/.warnyin/
/.claude/commands/warnyin/
/.claude/agents/
/CLAUDE.md
/AGENTS.md
