# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build (เผื่อ project จริงมาใช้ template นี้)
node_modules/
dist/
build/
*.log
*.tgz

# skill ที่ติดตั้ง local — ไม่ใช่ส่วนหนึ่งของ workflow ที่แจกจ่าย
.agents/
skills-lock.json

# === DOGFOOD layer (root) — gitignored ตาม docs/rule.md §6; source อยู่ src/** ===
# ★ root-anchored (/) เพื่อไม่ให้ match src/.warnyin, src/.claude (source ที่ต้อง publish)
# /.claude/ ครอบทั้ง commands/agents/skills/settings.local.json (root .claude = dogfood 100%)
/.warnyin/
/.claude/
/CLAUDE.md
/AGENTS.md
